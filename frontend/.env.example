# Local dev points at the FastAPI server on port 8000.
# Production builds can set this through the static host's environment
# variables to point at the deployed backend API.
VITE_API_BASE_URL=http://localhost:8000
